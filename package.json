{
  "name": "marlo-autodev",
  "version": "0.1.0",
  "private": true,
  "description": "Marlo app repo driven end to end by Agent OS: plan -> loop -> PR -> CI -> auto-merge -> Vercel.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "proof": "npm test"
  }
}
